[package]
name = "methods-guest"
version = "0.1.0"
edition = "2021"

[workspace]

[build-dependencies]
risc0-build = "3.0"

[dependencies]
risc0-zkvm = { version = "3.0", default-features = false }
sha2 = { version = "0.10.8", default-features = false }
